# 0.1.0 Windows Validation Record

Date: 2026-07-17

Status: accepted as G5 evidence under the
[G5 maintainer attestation](0.1.0-g5-attestation.md).

## Environment

- Windows 11 build `10.0.26200`
- `AMD Radeon(TM) 8060S Graphics`
- HIP runtime `amdhip64_7.dll`
- Validation source commit `20d40b0dc77a416a8bfe2d2c467b05295194837f`

## Network setup attempts

Earlier attempts timed out or stalled before receiving useful release data. On
2026-07-17, a fresh attempt reached GitHub and its release CDN but transferred
at approximately 27 KiB/s; it was stopped with a 9,437,184-byte partial file.
No checksum or installer failure was observed. The maintainer explicitly
waived empty-cache public-network transport for v0.1.0. This remains a
non-successful network attempt and is not represented as a pass.

## Verified-cache installation

Five existing local files were independently checked against the packaged
manifest. Every size and SHA-256 matched, including the 323,202,730-byte b9884
runtime archive, 935,769,056-byte main GGUF, 881,770,560-byte mmproj,
130,502,049-byte layout ONNX model, and 1,482-byte layout config. They were
copied into a new temporary root as a pre-verified local cache.

`paddleocr-vl-rocm setup --no-start --root <temporary-root>` then passed runtime
extraction/version verification and configuration activation. A full doctor run
reported:

- Windows, AMD GPU, HIP, config, disk, endpoint: PASS
- llama.cpp `9884 (86961efd5)`: PASS
- `Verified 5 pinned resources`: PASS
- PP-DocLayoutV3 activated with DirectML first and fallback disabled: PASS
- Port 8111: WARN because an existing intended server occupied it

## Onboarding inference

The managed-server path used the new installation on port 8111 and processed
`examples/input/magazine.png` into 44 blocks, JSON, and Markdown. After that
server was stopped, an independently started server on isolated port 8123
validated the existing-server path and produced the same 44 blocks, JSON, and
Markdown. Both test servers were terminated after validation.

These smoke runs validate installation activation, doctor checks, both server
journeys, and output generation on this machine. Separate evidence establishes
G0/G1/G3/G4. No clean public-network download is claimed.

# 0.1.0 Release Readiness

Status: READY

Audit date: 2026-07-17. This is a readiness record, not release notes and not an
authorization to publish. Accuracy, coverage, aggregation, provenance, and
performance claims are maintained in the
[OmniDocBench v1.6 benchmark fact sheet](../benchmarks/omnidocbench-v1.6.md).
G3 is accepted under the
[G3 maintainer attestation](0.1.0-g3-attestation.md): PaddleOCR confirmed
Overall 95.99 out of band, and the maintainer waived a public confirmation
artifact and another full run on 2026-07-17.

Release contract: Fresh official-local release evidence requires `count=1651`,
`ok=1650`, `fail=1`, `fallback=0`, and `limit_pages=null`; the sole approved
failure is `newspaper_The Times UK_0801@magazinesclubnew_page_031.png` with the
`peg-native` signature tracked at
https://github.com/PaddlePaddle/PaddleOCR/issues/18248. There is no failed-page
prediction file, and all 1,651 GT pages are scored.

| Gate | Status | Evidence |
|---|---|---|
| G0 evidence integrity | PASS | The independently reviewed score-only r7 run is bound by the tracked [G0 evidence receipt](0.1.0-g0-evidence.md), SHA-256 `d0b7fcbe389e03439b5ba65126008fa5ee828a59e358ae0347c5bb6a51648a04`, at producing commit `fd91cb0a2d75b0a18d16b1bb34652a148cb59b9e`. It authenticates immutable r5 inference with 1,650 successes, all six r7 Official outputs, outer runner exit 0, and no orphan. |
| G1 compatibility contract | PASS | CLI/API/default contracts, DirectML provider metadata, payload observation, trace hashing, JSON/Markdown contracts, and legacy CLI compatibility are covered by committed tests. |
| G3 accuracy acceptance | PASS | PaddleOCR confirmed Overall 95.99 out of band. The project maintainer accepted it, changed the G3 minimum to 95.99, and waived a public confirmation artifact and another full run in the tracked [G3 attestation](0.1.0-g3-attestation.md). |
| G4 performance acceptance | PASS | Against limits mean <=13.00 and P95 <=34.82 seconds/page, the [G4 diagnostic](0.1.0-g4-diagnostic.md) and tracked [final decision](0.1.0-g4-final-decision.json) establish mean 6.33, P95 19.54, 0/27 failures, and a targeted GT projection preserving Overall 95.99. Raw output equivalence remains explicitly false. |
| G5 launch readiness | PASS | The [G5 maintainer attestation](0.1.0-g5-attestation.md) accepts verified-cache installation, full Doctor, managed-server and existing-server inference, fresh wheel/sdist installs, and package/repository/security audits. The maintainer explicitly waived empty-cache public-network transport; no successful public download is claimed. |

G2 root-cause diagnosis is intentionally not part of this adaptation project's
release contract. It is diagnostic work, not a prerequisite for release.

GitHub authentication is currently available: `gh auth status` reports the
active `AIwork4me` account with `repo` and `workflow` scopes. Authentication is
transient operational state, must be checked again at publication time, and
does not authorize bypassing any evidence gate.

## Publication checks

1. Require the final branch's GitHub CI checks to pass.
2. Recheck GitHub authentication immediately before merge and publication.
3. Build release artifacts from the merged `main` commit and publish their
   checksums with the GitHub Release.

All release gates pass. Merge and publication remain separate, auditable
operations and must not bypass the publication checks above.

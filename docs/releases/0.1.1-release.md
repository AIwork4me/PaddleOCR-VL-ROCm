# 0.1.1 Patch Release Contract

Date: 2026-07-17

Status: **READY**

## Purpose

Version 0.1.1 is an evidence-alignment patch release. It includes the
post-publication G5 closeout that was merged after the immutable v0.1.0 tag.
It contains no inference, scoring, runtime, model, resource-manifest, or public
API behavior change.

The v0.1.0 tag and Release remain unchanged at
`afc9b65cb0c2f8d8effb1a4d22b8323bed1640ec`. Rewriting that public tag would
invalidate previously published artifact and source identities.

## Inherited release evidence

Version 0.1.1 inherits the accepted v0.1.0 release contracts:

- G0 evidence integrity: PASS
- G1 compatibility contract: PASS
- G3 accuracy: PASS at Overall 95.99
- G4 performance: PASS at mean 6.33 seconds/page and P95 19.54 seconds/page
- G5 launch readiness: PASS

The exact evidence boundaries remain in the
[v0.1.0 readiness record](0.1.0-readiness.md), including the known upstream
one-page exception, explicit raw-output non-equivalence disclosure, removal of
G2 as a release gate, and the maintainer's empty-cache public-network transport
waiver.

The [G5 post-publication closeout](0.1.0-g5-closeout.md) additionally records:

- a clean-worktree full check against the v0.1.0 merge commit;
- Twine validation of both v0.1.0 distributions;
- PR, CI, merge, tag, GitHub Release, asset, and checksum verification.

## Publication contract

The v0.1.1 tag and GitHub Release must:

1. be created from the merge commit containing this version bump and closeout;
2. leave v0.1.0 and its assets untouched;
3. use artifacts rebuilt from that exact v0.1.1 merge commit;
4. pass member-list auditing, fresh installation, import, metadata, manifest,
   CLI, and Twine checks;
5. attach SHA-256 checksums for the rebuilt wheel and source distribution;
6. publish only after all final pull-request CI jobs pass.

This patch release does not claim a successful empty-cache public-network
resource installation.

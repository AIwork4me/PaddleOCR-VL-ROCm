# 0.1.0 Publication Handoff

Recorded: 2026-07-17

Branch commit initially pushed after G5 acceptance:
`9f3a4a7238aacfb58abf3f492e61e92f1f349eba`.

## G5 items that remain intentionally incomplete

- Empty-cache public-network installation did not complete. The maintainer
  explicitly waived this transport check for v0.1.0; it is not reported as a
  pass and is not a release blocker.
- The final clean-worktree local `scripts/check.ps1` run was stopped on the
  maintainer's instruction instead of being awaited. Before it was stopped,
  Ruff, formatting, and mypy passed. A prior full pytest run reached 100% with
  no failures, and the changed documentation-contract suite passed 31/31.
  Final cross-platform acceptance is delegated to the required GitHub CI 6/6
  result on the final pull-request HEAD.
- `twine check` was unavailable in the validation environment. This is not a
  packaging blocker because both artifacts built, their member lists were
  audited, and the wheel and source distribution each installed and imported
  successfully in separate fresh virtual environments.

## Publication handoff

The remaining release operations are:

1. Require all six GitHub CI jobs to pass on the final PR HEAD.
2. Mark PR #3 ready and merge it into `main` without bypassing checks.
3. Rebuild wheel and source distribution from the merged `main` commit.
4. Recheck artifact contents and fresh installation, compute SHA-256 values,
   and publish them with GitHub Release `v0.1.0`.

Do not publish pre-merge artifacts: documentation changes alter distribution
metadata, so their hashes are validation evidence only.

## Post-release follow-up

- Improve mirror/offline installation guidance and eventually validate a
  successful empty-cache public download without rewriting the v0.1.0 waiver.
- Add managed-server stop/status/cleanup commands.
- Expand validation beyond the single recorded Windows AMD machine.
- Preserve the known upstream one-page exception, Overall 95.99, the G4 raw
  output non-equivalence disclosure, and the G2 non-gate decision.

Local benchmark data, the OmniDocBench checkout, logs, generated outputs, and
ad-hoc analysis scripts remain intentionally untracked and must not be added to
the release.

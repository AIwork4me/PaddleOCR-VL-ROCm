# 0.1.0 Publication Handoff

Recorded: 2026-07-17

Closeout status: **CLOSED**. The remaining local validation items and all
publication operations were subsequently completed. See the
[G5 post-publication closeout](0.1.0-g5-closeout.md).

Branch commit initially pushed after G5 acceptance:
`9f3a4a7238aacfb58abf3f492e61e92f1f349eba`.

## G5 closeout

- Empty-cache public-network installation did not complete. The maintainer
  explicitly waived this transport check for v0.1.0; it is not reported as a
  pass and is not a release blocker.
- A new clean-worktree `scripts/check.ps1` run against the released merge
  commit completed with exit code 0. Compileall, Ruff, formatting, mypy, and
  full pytest all passed.
- Twine 6.2.0 checked the exact released wheel and source distribution; both
  passed.

## Publication completion

All six GitHub CI jobs passed on PR #3's final head. The PR was marked ready and
merged as `afc9b65cb0c2f8d8effb1a4d22b8323bed1640ec`. Wheel and source
distribution artifacts were rebuilt from that merge commit, rechecked,
hashed, and published with GitHub Release `v0.1.0`. Remote `main` and the
release tag resolve to the same merge commit.

## Post-release follow-up

- Improve mirror/offline installation guidance and eventually validate a
  successful empty-cache public download without rewriting the v0.1.0 waiver.
- Add managed-server stop/status/cleanup commands.
- Expand validation beyond the single recorded Windows AMD machine.
- Preserve the known upstream one-page exception, Overall 95.99, the G4 raw
  output non-equivalence disclosure, and the G2 non-gate decision.

Local benchmark data, the OmniDocBench checkout, logs, generated outputs, and
ad-hoc analysis scripts remain intentionally untracked and must not be added to
the release.

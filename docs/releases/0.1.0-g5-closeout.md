# 0.1.0 G5 Post-Publication Closeout

Date: 2026-07-17

Status: **CLOSED**

This record closes the two validation items left open in the original
[publication handoff](0.1.0-handoff.md). It does not revise the maintainer's
empty-cache public-network transport waiver and does not claim that path
succeeded.

## Clean-worktree full check

The released `main` commit
`afc9b65cb0c2f8d8effb1a4d22b8323bed1640ec` was checked out into a new detached
Git worktree. A dedicated virtual environment was created with the repository
installed editable from that worktree. A module-origin probe confirmed that
both the interpreter and `paddleocr_vl_rocm` resolved inside the isolated
worktree rather than the maintainer's primary checkout.

The three tests that had previously exposed the foreign editable-install
origin were run first and passed:

- `test_worktree_venv_reports_worktree_module_origins`
- `test_exact_origin_probe_survives_windows_powershell_5_native_quoting`
- `test_full_stage_all_transaction_fault_matrix[candidate-write]`

The unmodified command
`powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/check.ps1`
then completed with exit code 0 in 875.3 seconds:

- compileall: PASS
- Ruff check: PASS
- Ruff format: PASS, 108 files already formatted
- mypy: PASS, no issues in 25 source files
- full pytest: PASS, 100% complete with no failures

The worktree was bound to the released commit before execution. Local
benchmark data, generated outputs, logs, and ad-hoc scripts from the primary
checkout were absent from this check.

## Distribution metadata check

Twine 6.2.0 was installed into a dedicated temporary environment and run
against the exact artifacts rebuilt from the merged `main` commit and uploaded
to GitHub Release `v0.1.0`.

| Artifact | SHA-256 | `twine check` |
|---|---|---|
| `paddleocr_vl_rocm-0.1.0-py3-none-any.whl` | `c7e84e244c2f041a6ce7429c288ea8c1c3d8724799f2de14768352b3476fdef3` | PASSED |
| `paddleocr_vl_rocm-0.1.0.tar.gz` | `5cd10e4905297812424f05042002f46a461612a9884b814f0eda362d20f81cdc` | PASSED |

GitHub reports the same digests for both uploaded assets. The attached
`SHA256SUMS.txt` has GitHub digest
`19b6bd5a1d5552e498dbfc755ce5aa9b8cf3073ae0d93466ca913c02eb9ea5a2`.

## Publication evidence

- PR [#3](https://github.com/AIwork4me/PaddleOCR-VL-ROCm/pull/3) final head:
  `224ff71f1bbb36171f207e621b61e5495b00809e`
- PR state: merged at `2026-07-17T13:47:14Z`
- Merge commit: `afc9b65cb0c2f8d8effb1a4d22b8323bed1640ec`
- Final PR checks: 6/6 PASS — Ubuntu Python 3.10 twice, Windows Python 3.10
  twice, and Windows Python 3.13 twice
- Remote `main` and tag `v0.1.0`: both resolve to the merge commit
- GitHub Release
  [`v0.1.0`](https://github.com/AIwork4me/PaddleOCR-VL-ROCm/releases/tag/v0.1.0):
  published at `2026-07-17T13:49:54Z`, not a draft, not a prerelease, with all
  three assets uploaded

## Remaining non-gate work

The following are post-release improvements, not incomplete G5 evidence:

- improve mirror and offline installation guidance;
- optionally validate a future empty-cache public download without rewriting
  the v0.1.0 waiver;
- add managed-server stop/status/cleanup commands;
- expand validation beyond the single recorded Windows AMD machine.

G5 has no remaining unwaived validation or publication item.

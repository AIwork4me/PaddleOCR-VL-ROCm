# 0.1.1 Publication Closeout

Date: 2026-07-17

Status: **CLOSED**

This record closes the
[v0.1.1 patch release contract](0.1.1-release.md). Version 0.1.1 is the
immutable release snapshot that includes the post-publication v0.1.0 evidence
alignment. It does not change inference, scoring, runtime, models, the resource
manifest, or public API behavior.

## Source and publication identity

- PR [#5](https://github.com/AIwork4me/PaddleOCR-VL-ROCm/pull/5) was merged at
  `2026-07-17T14:31:17Z`.
- Final PR head:
  `f32ba91f801f5d18319976d8c7498de91f3958cb`.
- Merge commit:
  `5a52e8331b25c7684fc586b4de4dc8106070555a`.
- Remote `main`, tag `v0.1.1`, and the GitHub Release target all resolve to
  that merge commit.
- GitHub Release
  [`v0.1.1`](https://github.com/AIwork4me/PaddleOCR-VL-ROCm/releases/tag/v0.1.1)
  was published at `2026-07-17T14:34:18Z`; it is neither a draft nor a
  prerelease.
- The existing `v0.1.0` tag and Release remain unchanged at
  `afc9b65cb0c2f8d8effb1a4d22b8323bed1640ec`.

## CI evidence

The final PR checks completed 6/6 successfully:

- Ubuntu Python 3.10, twice;
- Windows Python 3.10, twice;
- Windows Python 3.13, twice.

The post-merge `main` CI
[run 29588329564](https://github.com/AIwork4me/PaddleOCR-VL-ROCm/actions/runs/29588329564)
completed 3/3 successfully against the release commit:

- Ubuntu Python 3.10;
- Windows Python 3.10;
- Windows Python 3.13.

Each post-merge job completed compileall, Ruff lint and format checks, mypy,
the non-GPU/non-network/non-integration pytest suite, and distribution builds.

## Published artifacts

The wheel and source distribution were rebuilt from the exact merged release
commit before publication. The three uploaded Release assets were downloaded
again during closeout and independently checked.

| Asset | Size | SHA-256 |
|---|---:|---|
| `paddleocr_vl_rocm-0.1.1-py3-none-any.whl` | 54,538 bytes | `c843ff7615a7958ded06b275ba4c2b91ca35b082a6f464b5437762a2b9b39bec` |
| `paddleocr_vl_rocm-0.1.1.tar.gz` | 157,454 bytes | `9585b42ba03d08703673bd8135fd666ca8acfcf2e7911151276dfe07cff2c40e` |
| `SHA256SUMS.txt` | 204 bytes | `f5a0a321f69d19aef6bf650adf427b238c9dd923c9222c80a0adae9f809b59c8` |

The two distribution hashes exactly match the attached `SHA256SUMS.txt` and
the digests reported by GitHub.

## Artifact validation

- Twine 6.2.0: both distributions PASSED.
- Member audit: 32 wheel members and 95 source-distribution members.
- Forbidden-content audit: no local benchmark data, generated outputs, logs,
  OmniDocBench checkout, or ad-hoc maintainer scripts.
- Runtime package-data audit: exactly one `assets/runtime-manifest.json` in
  each distribution.
- Fresh wheel installation without a network dependency install: PASS.
- Installed distribution version: `0.1.1`.
- Package import resolved from the fresh environment: PASS.
- Installed runtime manifest lookup: PASS.
- Installed CLI `--help`: PASS with exit code 0.

The pre-publication release validation also covered distribution metadata,
fresh installation, import, manifest, CLI, documentation contracts, and static
quality checks. The closeout download verifies that the assets users receive
retain the approved identities.

## Release-gate disposition

- G0 evidence integrity: PASS.
- G1 compatibility contract: PASS.
- G3 accuracy: PASS at Overall 95.99, with the accepted upstream one-page
  exception unchanged.
- G4 performance: PASS at mean 6.33 seconds/page and P95 19.54 seconds/page.
- G5 launch readiness and publication: PASS.
- G2 remains intentionally outside the release gates.
- The explicit empty-cache public-network transport waiver remains unchanged;
  this release makes no claim that path succeeded.

There is no remaining unwaived v0.1.1 release-gate, artifact, checksum,
publication, or evidence-closeout item. Further work belongs to later feature
or documentation changes and must not move or rewrite the `v0.1.1` tag.

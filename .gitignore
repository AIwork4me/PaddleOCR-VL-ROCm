__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
dist/
build/
*.egg-info/
models/
outputs/
*.log
.env
.env.local
.worktrees/

tests/fixtures/_tmp_record/

# OmniDocBench evaluation artifacts (generated; keep out of git by default).
# Commit a score as a release artifact separately when needed.
eval/.omnidocbench/          # cloned OmniDocBench checkout + its deps
predictions/                 # per-page .md predictions (infer stage output)
results/omnidocbench/        # copied/symlinked score tables


# OmniDocBench eval data (large; via modelscope)
OmniDocBench_data/

# local (machine-specific) eval configs
eval/configs/run_*_local.yaml

predictions/
result/
